export default function ColorList({ vice }) {
  //const lis = colors.map((cols) => <li>{colors}</li>);
  return (
    <div>
    {/*<div>
      <p>Color List</p>
      <ul>
        {colors.map((c) => (
          <li style = {{color: c}}>{c}</li>
        ))}
      </ul>
        </div> */}
      <p>vice is : {vice}</p>
      </div>
  );
}
